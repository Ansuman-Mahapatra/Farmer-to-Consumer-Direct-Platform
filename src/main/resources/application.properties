spring.application.name=Farmer-to-Consumer Direct Platform
# MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://Ansuman:your_password@cluster0.hzne8mh.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# JPA Configuration (Optional - if you're using both MongoDB and JPA)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Email Configuration (Update with your email provider)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your-email@gmail.com
spring.mail.password=your-app-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT Configuration
jwt.secret=your-jwt-secret-key-at-least-256-bits-long-for-security
jwt.expiration=86400000

# Server Configuration
server.port=8080
server.servlet.context-path=/

# Logging Configuration
logging.level.com.yourpackage=DEBUG
logging.level.org.springframework.data.mongodb.core=DEBUG